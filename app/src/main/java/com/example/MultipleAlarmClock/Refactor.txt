a) UI Refactor : --

b) Code Refactor :--
    * also add force update the app , or ask user to update the app
    [X] redo the cancelAlarmByCancelingPendingIntent()
    * put a ask user for review after 5 days popup
    * redo the ask for alarm sheet(alarm detail enter sheet); like that popup is ugly --
         now we can either use the the sat sun mon day etc remainder for the weekly repeating alarm or we can give the user a custom
         functionality displaying where they are in the week(scroll in the list so it displays date from m,t,w,f,s,s eg if we at
         Friday then scroll to the end and show the Friday), horizontal list and ask them to select dates they want to schedule
         the alarms at, can do only 20 at max, and when the user selects the past date then tell them they can't;
         and give them a option to go see the cal and select multiple dates
    * make sure that the db will not fall or crash in the prod; migration etc, see how real apps does it

c)  later :--
       [X] abandon or work on multiple dates alarm, eg start form  today 9:00Pm to tomorrow 4:00 Am -- decided not to
       -- try adding the set same alarm on multiple week days form click like stock clock
       -- can't use my alarms as they are copyrighted, would need to use the alarms present in the system
       [X] a click on alarm value and edit feature, eg endtime and a time picker appears etc
       [X] [UI] stop for 10(prefer)/20 min button that can be used 3 times,
        * a restart for 30 min button, here we can create a temp value or alarm where (alarm was from 2:00--6:00) from now(7:00) to now + 30 min (7:30) , create a new
            alarm instead of making db changes as that will be easier
        * in alarmActivity store the new intents in a list and when clicking destroy ensure that the app does not have more
        intent if we do then start them/ use an animation to start them in current instance
          here we will do this in the service
        [X] in the alarm Service add a after 1 min or so cancel the current playing alarm
        [X] re-design app using material 3 expressive
        * now use the alarm Intent data class to tell if we have alarm with random sound or what
        * add ability to schedule multiple alarms (calender) or ability to add repeating alarms (like MONDAY, WED, SAT etc)
        * set alarm based on AlarmObject.Date and not take the alarmObject.StartTime alarmObject.EndTime, assert them to be right but take it from time
        * DB migration test
        * AlarmService check if the notification permission is denied or not (or a coro) if it is then send the user a notification and tell them to enable it
            1) also analytics 2) FYI there will be many notification of this, this is by desing , eg from startTime to endTime no of alarms notification , and we will tell user that a alarm was there
            on $CurrentTime but since the permission was not there we can't deliver it please enable the permission
        * allow user to schedule multiple alarms (like multiple dates)
        [X] fix the weGood/AlarmData ok as when we edit the alarm and the change the time/date(IDK) then I have to click the date again to force the ui to be blue and notred
        * we can make the splash screen like this one ->  https://djectstudio.com/ (the round circle icon splash and changes the colour)
        * the value from the shared preference should be an Data class (serializable) , and when should take it out
            also there should be a app or per alarm setting if important then if the sound is slow then raise it
        * (Later after design and impl is done)haptic feedback
        * include tests on the main  branch and build only if it passes
        * integrate with Gemini or assistant, such that you can ask the LLM to schedule you an alarm



d) Publishing
    * have to change the alarm sound, should be using the system's alarm clock sound, -- now you will need to clean up your proguard file
    [X] Initial screen when fetching the alarms
    * maybe a initial signup screen, like ask user what are they using the app for , and how can we help them, productivity, distracton, Alarm
        or all, explain them how can we help, maybe a/b test this
     ask users for review on play store helps to boost app
    [X] analytics -- try post-hog or google ad mob
    * app permissions and justification screen, like I would need to tell the user to give me the notification and bg autostart permission
      and explain why I need it, and also check the actual permission in the app and not just some db field, like notification etc in seperate coro
      as if user or system revoked it we would no be able to display the alarm
    [X] hey we can also like shake it alarm display ads on top of the alarmActivity

e) after mvp --
    * maybe like slice like reward points and  spend points for using the app, watch ads or pay to get more pionts, full 8 months version also
    * a/b test the intrusive ads, and clutter free ads version, (like ads after every click , and point based one), if have more than 1000 users,
        -- see the retention etc, and see what type of users are willing to watch the ads
        there should be a way to a/b test this on some users based on their profiles etc
    * launch adds or play store and try to put it on top of search result
